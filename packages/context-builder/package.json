{
  "name": "@shadowbox/context-builder",
  "version": "0.1.0",
  "description": "Deterministic LLM context assembly engine for Shadowbox",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./types": "./dist/types.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shadowbox/intent-classifier": "workspace:*",
    "@shadowbox/repo-awareness": "workspace:*",
    "@shadowbox/token-budgeting": "workspace:*",
    "js-tiktoken": "^1.0.14"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
