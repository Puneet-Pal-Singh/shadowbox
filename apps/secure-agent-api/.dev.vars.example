# Secure API local development env example.

# Allow localhost/127.0.0.1/::1 origins in local dev.
CORS_ALLOW_DEV_ORIGINS=true

# Explicit allow-list for non-dev environments (comma-separated).
# Example: https://app.example.com,https://api.example.com
CORS_ALLOWED_ORIGINS=
