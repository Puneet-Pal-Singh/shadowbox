/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "shadowbox-api",
	"main": "src/index.ts",
	"compatibility_date": "2025-05-06",
	"limits": {
		"cpu_ms": 60000
	},
	"compatibility_flags": [
		"nodejs_compat"
	],
	"observability": {
		"enabled": true
	},
	"containers": [
		{
			"class_name": "Sandbox",
			"image": "./Dockerfile",
			"instance_type": "lite",
			"max_instances": 1
		}
	],
	"durable_objects": {
		"bindings": [
			{
				"name": "AGENT_RUNTIME",
				"class_name": "AgentRuntime"
			},
			{
				"name": "Sandbox",
				"class_name": "Sandbox"
			},
			{
				"name": "LAUNCH_RATE_LIMITER",
				"class_name": "LaunchRateLimiter"
			}
		]
	},
	"migrations": [
		{
			"tag": "v3", // Increment tag
			"new_sqlite_classes": ["AgentRuntime", "Sandbox"]
		},
		{
			"tag": "v4-launch-rate-limiter",
			"new_sqlite_classes": ["LaunchRateLimiter"]
		}
	],
	"r2_buckets": [
		{
			"binding": "ARTIFACTS",
			"bucket_name": "shadowbox-artifacts",
			"remote": true
		}
	],
	/**
	 * Smart Placement
	 * https://developers.cloudflare.com/workers/configuration/smart-placement/#smart-placement
	 */
	// "placement": {  "mode": "smart" }
	/**
	 * Bindings
	 * Bindings allow your Worker to interact with resources on the Cloudflare Developer Platform, including
	 * databases, object storage, AI inference, real-time communication and more.
	 * https://developers.cloudflare.com/workers/runtime-apis/bindings/
	 */
	/**
	 * Environment Variables
	 * https://developers.cloudflare.com/workers/wrangler/configuration/#environment-variables
	 * 
	 * For local development, create a .dev.vars file in this directory.
	 * See .dev.vars.example for required configuration.
	 * 
	 * CORS Configuration:
	 * - CORS_ALLOWED_ORIGINS: Comma-separated list of allowed origins for production
	 * - CORS_ALLOW_DEV_ORIGINS: Set to "true" to allow localhost/127.0.0.1/::1 in development
	 * - LAUNCH_EMERGENCY_SHUTOFF_MODE: off | block_session_and_execute | block_all
	 * - SESSION_CREATE_RATE_LIMIT_AUTH_MAX: default 20 (10-min window)
	 * - SESSION_CREATE_RATE_LIMIT_ANON_MAX: default 10 (10-min window)
	 * - SESSION_CREATE_RATE_LIMIT_WINDOW_SECONDS: default 600
	 * - EXECUTE_TASK_RATE_LIMIT_AUTH_MAX: default 120 (10-min window)
	 * - EXECUTE_TASK_RATE_LIMIT_ANON_MAX: default 20 (10-min window)
	 * - EXECUTE_TASK_RATE_LIMIT_WINDOW_SECONDS: default 600
	 *
	 * Production example (LegionCode launch):
	 * - CORS_ALLOWED_ORIGINS=https://legioncode.dev,https://staging.legioncode.dev
	 * 
	 * Note: Use secrets to store sensitive data.
	 * https://developers.cloudflare.com/workers/configuration/secrets/
	 */
	"vars": {
		"CORS_ALLOW_DEV_ORIGINS": "false"
	}
	/**
	 * Static Assets
	 * https://developers.cloudflare.com/workers/static-assets/binding/
	 */
	// "assets": {  "directory": "./public/",  "binding": "ASSETS" }
	/**
	 * Service Bindings (communicate between multiple Workers)
	 * https://developers.cloudflare.com/workers/wrangler/configuration/#service-bindings
	 */
	// "services": [  {   "binding": "MY_SERVICE",   "service": "my-service"  } ]
}
